Drop a soft instrumental song here, named `melody.mp3`, and it will
start playing softly (fade-in) right after the booklet is opened.

It never autoplays — it only starts after the user clicks the cover,
and can be paused with the little music note in the corner.

Set `music.src` to '' in `src/config/content.js` to disable music entirely.